# IoT Room Flutter Client

Flutter 客户端用于展示设备状态、历史曲线、远程控制、视频监控和告警信息。配置与启动方式请查看仓库根目录 [README](../README.md)。

客户端通过 `--dart-define` 读取环境参数，配置定义位于 `lib/core/config/app_config.dart`。API地址、设备编号和访问参数可以按部署环境注入。
